# =============================================================================
# .env.local.example
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# .env.local is git-ignored for security

# =============================================================================
# Next.js Configuration
# =============================================================================
NODE_ENV=development

# Image domains allowed for Next.js Image optimization
# Comma-separated list of domains
NEXT_PUBLIC_IMAGE_DOMAINS=localhost,dev.brianfending.com,brianfending.com

# =============================================================================
# Google Analytics
# =============================================================================
# Google Analytics 4 Measurement ID
# Format: G-XXXXXXXXXX
# Only loads in production, safe to leave blank for development
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# =============================================================================
# reCAPTCHA v3 Configuration
# =============================================================================
# Get these from https://www.google.com/recaptcha/admin
# reCAPTCHA v3 site key (public, safe to expose in frontend)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your-recaptcha-site-key-here

# reCAPTCHA v3 secret key (private, server-side only)
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key-here

# =============================================================================
# Google Sheets Integration (Contact Form) - DEPRECATED, use Jira instead
# =============================================================================
# Service account email from Google Cloud Console
GOOGLE_SHEETS_CLIENT_EMAIL=your-service-account@project-id.iam.gserviceaccount.com

# Service account private key from Google Cloud Console
# Include the full key with newlines as \n
GOOGLE_SHEETS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key content here\n-----END PRIVATE KEY-----\n"

# Google Sheet ID from the URL
# https://docs.google.com/spreadsheets/d/[SHEET_ID]/edit
GOOGLE_SHEETS_SHEET_ID=your-google-sheet-id-here

# =============================================================================
# Jira Integration (Contact Form)
# =============================================================================
# Jira connection configuration
JIRA_PROTOCOL=https
JIRA_HOST=your-domain.atlassian.net
JIRA_USERNAME=your-email@domain.com
JIRA_API_TOKEN=your-jira-api-token

# Jira project and issue configuration
JIRA_PROJECT_KEY=your-project-key
JIRA_ISSUE_TYPE=Task

# =============================================================================
# Content Repository Configuration
# =============================================================================
# Base URL for fetching content from GitHub
# Used for dynamic content loading from bfdc-content repository
GITHUB_BASE_URL=https://raw.githubusercontent.com/fending/bfdc-content/main

# =============================================================================
# Podcast & Video Automation (Future Features)
# =============================================================================
# These are used in GitHub Actions workflows for content automation
# Leave blank for local development

# Acast Podcast Hosting
ACAST_API_KEY=your-acast-api-key
ACAST_SHOW_ID=your-acast-show-id

# YouTube API for video/shorts publishing
YOUTUBE_CLIENT_ID=your-youtube-oauth-client-id
YOUTUBE_CLIENT_SECRET=your-youtube-oauth-client-secret
YOUTUBE_REFRESH_TOKEN=your-youtube-refresh-token

# Bluesky Social Platform
BLUESKY_EMAIL=your-bluesky-account-email
BLUESKY_PASSWORD=your-bluesky-account-password

# Repository access for automated workflows
REPO_ACCESS_TOKEN=your-github-token-with-repo-dispatch-permissions

# =============================================================================
# Development Notes
# =============================================================================
# 
# Required for Local Development:
# - NEXT_PUBLIC_RECAPTCHA_SITE_KEY (contact form will not work without this)
# - RECAPTCHA_SECRET_KEY (contact form will not work without this)
# - GOOGLE_SHEETS_* variables (contact form submissions will fail without these)
#
# Optional for Local Development:
# - NEXT_PUBLIC_GA_MEASUREMENT_ID (analytics disabled in development)
# - NEXT_PUBLIC_IMAGE_DOMAINS (fallback domains are configured)
# - Podcast/Video automation variables (used in production workflows only)
#
# How to get required credentials:
# 
# 1. reCAPTCHA v3:
#    - Go to https://www.google.com/recaptcha/admin
#    - Create a new site with reCAPTCHA v3
#    - Add your domains (localhost for development)
#    - Copy the site key and secret key
#
# 2. Google Sheets API:
#    - Go to Google Cloud Console
#    - Create a new project or select existing
#    - Enable Google Sheets API
#    - Create a service account
#    - Download the JSON key file
#    - Extract client_email and private_key from the JSON
#    - Create a Google Sheet and share it with the service account email
#    - Copy the sheet ID from the URL
#
# 3. Google Analytics (optional):
#    - Go to Google Analytics
#    - Create a GA4 property
#    - Copy the Measurement ID (starts with G-)
#
# Security Notes:
# - Never commit .env.local to git
# - Keep your private keys secure
# - Use different credentials for development and production
# - Regularly rotate API keys and tokens